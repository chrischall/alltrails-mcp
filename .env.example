# Optional. A Cookie header copied from a signed-in alltrails.com browser tab
# (must include the short-lived `datadome` cookie). If unset, the server falls
# back to the fetchproxy browser extension. See README for details.
ALLTRAILS_COOKIE=
